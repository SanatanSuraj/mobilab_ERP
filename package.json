{
  "name": "mobilab-erp",
  "version": "0.1.0",
  "private": true,
  "description": "Mobilab ERP — unified monorepo for CRM/Production/Inventory/QC/Procurement/Finance",
  "packageManager": "pnpm@9.14.2",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=9.0.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*",
    "tests/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "test:gates": "pnpm --filter @mobilab/gates test",
    "db:migrate": "pnpm --filter @mobilab/db migrate:up",
    "infra:up": "docker compose -f ops/compose/docker-compose.dev.yml up -d",
    "infra:down": "docker compose -f ops/compose/docker-compose.dev.yml down",
    "infra:logs": "docker compose -f ops/compose/docker-compose.dev.yml logs -f",
    "infra:reset": "docker compose -f ops/compose/docker-compose.dev.yml down -v"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "turbo": "^2.3.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.5"
  }
}
